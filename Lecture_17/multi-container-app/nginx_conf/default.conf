# server {
#     listen 80;
#     server_name localhost;

#     location / {
#          root /usr/share/nginx/html;
#         index index.html;
#     }
# }
http {
    upstream web_servers {
        server web:80;  # The service name defined in docker-compose
        server web:80;
        server web:80;
    }

    server {
        listen 80;

        location / {
            proxy_pass http://web_servers;
        }
    }
}
